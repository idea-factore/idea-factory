import { createMuiTheme } from '@material-ui/core/styles';

export const themeOptions = createMuiTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#e0e0e0',
    },
    secondary: {
      main: '#616161',
    },
  },
});